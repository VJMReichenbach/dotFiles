;; kanata config file
;; :set filetype=lisp
;; Caps to escape/control configuration for Kanata

(defsrc
  caps
  esc
)

(defalias
  escctrl (tap-hold 100 100 esc lmet)
)


(deflayer base
  @escctrl
  caps
)
